# Types
type Location {
  id: ID!
  name: String!
  address: String!
}

# Inputs
input LocationInput {
  name: String!
  address: String!
}

input LocationEdit {
  id: ID!
  name: String
  address: String
}

input LocationFilters {
  limit: Int
}
