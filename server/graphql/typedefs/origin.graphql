# Types
type Origin {
  id: ID!
  municipality: String!
  community: String!
  group: Int!
  code: String!
}

# Inputs
input OriginInput {
  municipality: String!
  community: String!
  group: Int!
}

input OriginEdit {
  id: ID!
  municipality: String
  community: String
  group: Int
}

input OriginFilters {
  limit: Int
}
